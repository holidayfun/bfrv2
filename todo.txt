p4 code vervollständigen
    -default drop, falls kein BS präfix gefunden wird
    -cloning des packets, entsprechend bs anpassen
    -übergabe an multicast overlay anpassen
    -add_bier_header dynamisch gestalten, mit recirculation
    
skript schreiben, um bit index forwarding table zu füllen

skript schreiben, um check_bfr_id table zu füllen


